[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "topology-format-converter"
version = "0.1.0"
description = "Format conversion utilities for SELTO topology optimization data, voxel volumes, SDF caches, and meshes."
readme = "README.md"
requires-python = ">=3.9"
authors = [
  { name = "Topology Format Converter contributors" }
]
dependencies = [
  "numpy",
  "scipy",
  "trimesh",
  "scikit-image"
]

[project.optional-dependencies]
dev = ["pytest"]
meshio = ["meshio"]
selto = ["torch"]

[project.scripts]
topology-convert = "topology_format_converter.cli:main"

[tool.setuptools.packages.find]
where = ["."]
include = ["topology_format_converter*"]
